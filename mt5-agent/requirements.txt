# VECTOR AI Trading System — Dependencies
# Install: pip install -r requirements.txt

# Core
MetaTrader5>=5.0.45          # MT5 Python API (Windows only)
python-dotenv>=1.0.0          # .env file loading

# Async & HTTP
httpx>=0.27.0                 # async HTTP client
asyncio-mqtt>=0.16.0          # optional: MQTT for inter-agent

# FastAPI Dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0

# Data Processing
pandas>=2.2.0
numpy>=1.26.0

# ML (Self-Learning Agent)
scikit-learn>=1.4.0

# Telegram
# (uses httpx directly, no additional package needed)

# PDF Reports (optional)
reportlab>=4.1.0              # for PDF weekly reports
matplotlib>=3.8.0             # for equity curve charts
plotly>=5.20.0                # for interactive charts

# Utilities
python-dateutil>=2.9.0
pytz>=2024.1
